// expression ? ‘if true’ : 'if false'
let age = 25;
let access = age > 17 ? 'Allow entry': 'Do not allow entry';

console.log(access);
/*
The function of the ternary operator is similar to the
if flow control operator,
since where the ternary operator evaluates to
true, it returns the first option in line 3,
otherwise, it will return the second option because it will evaluate to
false.
*/