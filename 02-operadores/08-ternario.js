// expresión ? 'si es true' : 'si es false'
let edad = 25;
let acceso = edad > 17 ? 'Permitir ingreso' : 'No permitir ingreso';

console.log(acceso);
// la función del operador ternario es similar al
// operador de control de flujo if,
// puesto que donde el operador ternario evalúa
// true, devuelve la primera opción de la línea 3,
// si no, devolverá la segunda opción porque evaluará
// false.